<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<select id="search" parameterType="member.MemberVo" resultType="member.MemberVo">
		select * from member111 where mid= #{findMid} and pwd = #{findPwd}	
	</select>

</mapper>