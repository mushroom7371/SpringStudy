<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="reserve">
	<!-- 입력 -->
	<insert id="reserve_insert" parameterType="reserve.MyReserveVo">
			INSERT INTO MyReserve(myName, myJumin, myPhone, Email, reserveVaccine, reserveTime, reserveCenter, reserveNum )
			values(#{myName}, #{myJumin} , #{myPhone}, #{Email}, #{reserveVaccine},  #{reserveTime},  #{reserveCenter},  #{reserveNum})		
	</insert>
	
</mapper>